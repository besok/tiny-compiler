func main(args []string) []num {
    var arr []num = [..]num
    Add(arr,1)
    Add(arr,2)
    Add(arr,3)
    Add(arr,4)
   for(var i num = 0; i < Len(arr); i = i+1){
    var s string = "el=" + i
    Output(s)
   }
}